on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

name: R-CMD-check

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}
    name: ${{ matrix.config.os }} (R ${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          - { os: ubuntu-latest,  r: release  }
          - { os: ubuntu-latest,  r: devel,   http-user-agent: release }
          - { os: ubuntu-latest,  r: oldrel-1 }
          - { os: macos-latest,   r: release  }
          - { os: windows-latest, r: release  }

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      _R_CHECK_FORCE_SUGGESTS_: false

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      # RSPM has no binaries for R-devel; setup-r@v2 restores release-compiled
      # binaries whose .so files reference removed R internals (e.g. SETLENGTH).
      # Clear the cached contents but recreate the directory so R still has a
      # writable library to install fresh compatible packages into.
      - name: Clear setup-r temp library on Linux
        if: runner.os == 'Linux'
        run: |
          rm -rf "${RUNNER_TEMP}/Library"
          mkdir -p "${RUNNER_TEMP}/Library"
        shell: bash

      - name: Install dependencies and check
        run: |
          install.packages(c("remotes", "rcmdcheck", "testthat", "devtools", "covr", "xml2",
                             "plotly", "jsonlite", "htmlwidgets"))
          remotes::install_deps(
            pkgdir       = ".",
            dependencies = c("Depends", "Imports", "LinkingTo")
          )
          rcmdcheck::rcmdcheck(
            args     = c("--no-manual", "--as-cran"),
            error_on = "warning"
          )
        shell: Rscript {0}
