% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_waldTest.R
\name{plot_waldTest}
\alias{plot_waldTest}
\title{Forest Plot for Wald Test Results}
\usage{
plot_waldTest(
  res,
  facet = TRUE,
  ci_level = 0.95,
  alpha = 0.05,
  theme = ggplot2::theme_bw(),
  return_data = FALSE,
  ...
)
}
\arguments{
\item{res}{List returned by \code{\link[=waldTest]{waldTest()}}.}

\item{facet}{Logical.  When \code{\link[=waldTest]{waldTest()}} was called with a \code{by}
argument, \code{TRUE} (default) produces one facet panel per group with free
y-scales; \code{FALSE} places all groups on a single panel separated by dotted
horizontal lines.}

\item{ci_level}{Numeric in (0, 1).  Confidence level for the CI arms on
Contrast rows.  Default \code{0.95}.}

\item{alpha}{Numeric significance threshold.  Controls the colour-scale
break between non-significant (grey) and significant (warm gradient)
results, and the \code{significant} column in the returned data frame.
Default \code{0.05}.}

\item{theme}{A ggplot2 theme object used as the base theme.
Default \code{\link[ggplot2:ggtheme]{ggplot2::theme_bw()}}.}

\item{return_data}{Logical.  If \code{TRUE}, returns the tidy data frame used to
build the plot rather than the ggplot object.  Useful for bespoke
customisation.  Default \code{FALSE}.}

\item{...}{Reserved for future use.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object returned invisibly (so it can be extended
with \code{+}), or a data frame when \code{return_data = TRUE}.
}
\description{
Produces a forest plot from the output of \code{\link[=waldTest]{waldTest()}}.

\strong{Contrast rows} are drawn as filled circles (\u25CF) with horizontal
confidence interval bars.  \strong{Joint zero test rows} are drawn as filled
diamonds (\u25C6), anchored at \eqn{x = 0} with no CI bar, on a light-blue
background band to make them immediately distinguishable.

Points are coloured by \eqn{-\log_{10}(p)}: non-significant results
(p \eqn{\geq} \code{alpha}) appear in grey; significant results follow a warm
gradient from gold through to dark red as evidence strengthens.  The raw
p-value is printed beside each row.

A vertical dashed line marks \eqn{x = 0}.
}
\examples{
\dontrun{
## Pairwise contrasts + joint zero test, no by-group
res <- waldTest(pred,
  cc = list(
    list(coef = c("N0","N1","N2"), type = "con",  comp = "pairwise"),
    list(coef = c("N1","N2"),      type = "zero",  group = "N1:N2 joint")
  ))
plot_waldTest(res)

## By-group (one panel per site, default)
res2 <- waldTest(pred_site,
  cc = list(list(coef = c("N0","N1","N2"), type = "con", comp = "pairwise")),
  by = "Site")
plot_waldTest(res2, facet = TRUE)

## By-group — all sites on one panel with separator lines
plot_waldTest(res2, facet = FALSE)

## 99\% CI
plot_waldTest(res, ci_level = 0.99)

## Stricter significance threshold
plot_waldTest(res, alpha = 0.01)

## Return tidy data for bespoke use
df <- plot_waldTest(res, return_data = TRUE)
}

}
\seealso{
\code{\link[=waldTest]{waldTest()}}
}
