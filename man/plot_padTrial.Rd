% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_padTrial.R
\name{plot_padTrial}
\alias{plot_padTrial}
\title{Before/After Field Layout Plot for padTrial() Results}
\usage{
plot_padTrial(
  result,
  data = NULL,
  type_col = "Type",
  pattern = "Row:Column",
  split = NULL,
  label = NULL,
  theme = ggplot2::theme_bw(),
  return_data = FALSE,
  ...
)
}
\arguments{
\item{result}{Data frame returned by \code{\link[=padTrial]{padTrial()}}. Must contain an
\code{add} column with values \code{"old"} and \code{"new"}.}

\item{data}{Optional original data frame passed to \code{\link[=padTrial]{padTrial()}}.
When supplied the Before panel shows the complete original layout
including guard rows. When \code{NULL} (default) the Before panel is
reconstructed from the \code{"old"} rows of \code{result}.}

\item{type_col}{Character. Name of the plot-type column used to colour
tiles. Default \code{"Type"}.}

\item{pattern}{Character. Colon-separated names of the row and column
coordinate columns, e.g. \code{"Row:Column"}. Default \code{"Row:Column"}.}

\item{split}{Character vector of column name(s) matching the \code{split}
argument used in \code{\link[=padTrial]{padTrial()}}. Groups appear as columns in the facet
grid. Default \code{NULL} (no grouping).}

\item{label}{Character or \code{NULL}. Name of a column whose values are
printed as text inside each tile. \code{NULL} (default) produces no labels.}

\item{theme}{A ggplot2 theme object. Default \code{\link[ggplot2:ggtheme]{ggplot2::theme_bw()}}.}

\item{return_data}{Logical. If \code{TRUE} returns the tidy data frame used to
build the plot. Default \code{FALSE}.}

\item{...}{Reserved for future use.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object returned invisibly (extendable with \code{+}),
or a data frame when \code{return_data = TRUE}.
}
\description{
Produces a tile map showing the field trial layout \strong{before} and \strong{after}
\code{\link[=padTrial]{padTrial()}} is applied.

The \strong{Before} panel shows the original layout. If \code{data} is supplied this
includes guard rows and any cells outside the bounding box; if \code{data} is
\code{NULL} (default) the Before panel is reconstructed from the retained
(\code{"old"}) rows of \code{result} and gaps appear as white space where missing
cells were located.

The \strong{After} panel shows the padded result. Inserted cells (\code{add == "new"})
are shown in light grey so they are immediately distinguishable from
original plots.

Tiles are coloured by the plot-type column. When \code{split} is supplied,
groups appear as columns in a facet grid with Before/After as rows, keeping
Before always on top.
}
\examples{
\dontrun{
## Basic use — Before panel reconstructed from result
res <- padTrial(trial_data, split = "Block", verbose = FALSE)
plot_padTrial(res)

## Supply original data to include guard rows in Before panel
plot_padTrial(res, data = trial_data)

## Multi-block split — groups as columns
plot_padTrial(res, data = trial_data, split = "Block")

## Label each tile with the genotype name
plot_padTrial(res, label = "Geno")

## Return tidy data for bespoke customisation
df <- plot_padTrial(res, return_data = TRUE)
}

}
\seealso{
\code{\link[=padTrial]{padTrial()}}
}
