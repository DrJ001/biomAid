% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fastIC.R
\name{plot_fastIC}
\alias{plot_fastIC}
\title{Plot Output from fastIC()}
\usage{
plot_fastIC(
  res,
  type = c("fast", "biplot", "CVE", "VAF", "iclass", "OP.pairs", "OP.variety"),
  highlight = "default",
  n_highlight = 3L,
  theme = ggplot2::theme_bw(),
  return_data = FALSE,
  ...
)
}
\arguments{
\item{res}{A long-format data frame returned by \code{\link[=fastIC]{fastIC()}}.}

\item{type}{Character; the visualisation to produce. One of
\code{"fast"} (default), \code{"biplot"}, \code{"CVE"}, \code{"VAF"}, \code{"iclass"},
\code{"OP.pairs"}, \code{"OP.variety"}.  May be abbreviated.}

\item{highlight}{Controls variety annotation. One of \code{"default"}
(automatic; see Details), a character vector of variety names, or \code{NULL}
(no annotation).  Default \code{"default"}.}

\item{n_highlight}{Positive integer. Maximum number of genotypes to
highlight automatically when \code{highlight = "default"}. Default \code{3L}.}

\item{theme}{A complete ggplot2 theme object. Default
\code{\link[ggplot2:ggtheme]{ggplot2::theme_bw()}}.}

\item{return_data}{Logical. If \code{TRUE}, returns a list with elements \code{plot}
(the ggplot object) and \code{data} (the underlying tidy data). Default
\code{FALSE}.}

\item{...}{Reserved for future use; currently ignored.}
}
\value{
When \code{return_data = FALSE} (default): a \code{ggplot} object.
When \code{return_data = TRUE}: a named list with elements \code{plot} and \code{data}.
The structure of \code{data} is type-specific — see the Details section for
each plot type.
}
\description{
Produces one of several ggplot2 visualisations from a \code{\link[=fastIC]{fastIC()}} result
object.  The function dispatches to a specialised plot builder depending
on the \code{type} argument and returns either a \code{ggplot} object or a list
containing the plot and the underlying tidy data.
}
\details{
\subsection{Plot types}{

\describe{
\item{\code{"fast"}}{Scatter of global Overall Performance (\code{global_op}) vs
global stability (\code{global_stab}) with quadrant annotations.}
\item{\code{"biplot"}}{Factor-analysis biplot with genotype score points and
environment loading arrows (Factors 1 & 2).  Requires k \eqn{\geq} 2.}
\item{\code{"CVE"}}{Diverging-colour heatmap of the Common Variety Effect
(genotype \eqn{\times} environment), ordered by iClass then
first-factor loading.}
\item{\code{"VAF"}}{Stacked 100\\% bar chart of Variance Accounted For per
environment.  Each bar is subdivided by FA factor (blues, bottom to top)
plus specific variance (grey, top).  A dashed line marks the
overall (across-environment) mean proportion explained.  Environments
are ordered to match the \code{"CVE"} heatmap (iClass then first-factor
loading descending).  Requires the \code{vaf_env} attribute produced by
\code{\link[=fastIC]{fastIC()}}.}
\item{\code{"iclass"}}{Scatter of within-class OP vs within-class RMSD,
faceted by iClass.}
\item{\code{"OP.pairs"}}{Lower-triangular pairs plot of iClassOP values across
all iClass levels.  Uses \pkg{patchwork} if available; falls back to
\code{facet_grid}.  Requires \eqn{\geq} 2 iClass levels.}
\item{\code{"OP.variety"}}{Line plot of iClassOP across ordered interaction
classes for highlighted varieties, with all others as a grey background.}
}
}

\subsection{Highlighting}{

When \code{highlight = "default"} the function automatically selects genotypes
to annotate based on the plot type:
\itemize{
\item For \code{"fast"}, \code{"biplot"}, \code{"CVE"}: top \code{n_highlight} by
\code{global_op} \emph{and} top \code{n_highlight} by instability (\code{global_stab}),
deduplicated.
\item For \code{"VAF"}: highlighting is not applicable (environment-level plot).
\item For \code{"iclass"}, \code{"OP.pairs"}, \code{"OP.variety"}: top \code{n_highlight} by
mean iClassOP across classes (or by \code{global_op} as fallback).
}
Supply \code{highlight = NULL} to suppress all annotation, or a character vector
of variety names to annotate specific genotypes.
}

\subsection{Dependencies}{

Annotation labels use \pkg{ggrepel} when available (non-overlapping), with
a fallback to \code{geom_text}.  The \code{"pairs"} plot uses \pkg{patchwork} when
available for individual panel assembly.  Colour palettes come from
\pkg{scales}.
}
}
\examples{
\dontrun{
res <- fastIC(model, ic.num = 2L)

# FAST scatter
plot_fastIC(res, type = "fast")

# FA biplot with custom highlights
plot_fastIC(res, type = "biplot", highlight = c("Var01", "Var22"))

# CVE heatmap, retrieve data
out <- plot_fastIC(res, type = "CVE", return_data = TRUE)
head(out$data)

# VAF stacked bar chart
plot_fastIC(res, type = "VAF")

# iClassOP trajectory for top 5 varieties
plot_fastIC(res, type = "OP.variety", n_highlight = 5L)

# Lower-triangular iClassOP pairs plot
plot_fastIC(res, type = "OP.pairs")
}

}
\seealso{
\code{\link[=fastIC]{fastIC()}}, \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}
}
